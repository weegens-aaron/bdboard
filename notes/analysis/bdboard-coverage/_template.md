# <Capability Area> — Coverage Findings

> **Section template — DO NOT edit this file to record findings.**
> Copy this shape into the matching `NN-<area>.md` section file and fill it in.
> Leading-underscore files are excluded from the rendered docs index, so this
> template never ships as a "finding."

| Field            | Value                                                              |
| ---------------- | ----------------------------------------------------------------- |
| Capability area  | `<area slug>`                                                     |
| Field-guide ref  | `field-guide-NN-<chapter>.html` (chapter N)                       |
| bdboard owner    | `bdboard-XXXX` (the audit bead that fills this section)            |
| Primary sources  | `src/bdboard/*.py`, `templates/partials/*.html`, `docs/catalog/*.md` |
| Status           | `not-started` \| `in-progress` \| `done`                          |

---

## 1. AVAILABLE — what the field guide documents

What does bd 1.0.4 actually expose for this capability? Summarize the feature
surface **with a chapter reference** so a reader can trace the claim.

- Cite the chapter: `field-guide-NN-<chapter>.html § "<section heading>"`.
- One bullet per discrete feature/sub-command/field/flag.
- Stick to what the guide *says exists* — not what bdboard does with it.

## 2. REFLECTED — what bdboard actually displays

What does bdboard **show the user** of the above? **Every claim needs a
`file:line` citation** into the bdboard display surface so the synthesis is
verifiable. Cite `src/bdboard/*.py` (derivation/routes), `templates/partials/*.html`
(the rendered markup), and `docs/catalog/*.md` (the feature's own documentation).

- `derive/lanes.py:NN` — how the bead state is bucketed/derived for display…
- `templates/partials/field_row.html:NN` — the markup that renders the field…
- `docs/catalog/<feature>.md` — what the catalog says this feature shows.
- If a feature is consumed/derived in `bd.py`, cite the call site.
- If a feature is **not** reflected at all, say so explicitly here (and open a
  GAP below) — silence is not a finding.

## 3. GAPS — what's misrepresented or unshown, and how much it matters

One row per gap. Severity is **P0–P4** (P0 = display misrepresents state so a
user decides wrong; P4 = cosmetic). Each gap gets a one-line recommended
follow-up that a future bead could be filed from verbatim.

| #   | Gap (one line)                                  | Severity | Recommended follow-up (one line)               |
| --- | ----------------------------------------------- | -------- | ---------------------------------------------- |
| 1   | `<what bdboard misrepresents / fails to show>`  | P2       | `<file a bead to … >`                          |
| 2   |                                                 |          |                                                |

### Severity rubric (DISPLAY tool)

| Sev | Meaning                                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------- |
| P0  | Display **misrepresents** bead state so a user decides wrong (e.g. blocked shown as Ready; reversed dependency direction — cf. `bdboard-fjk`). |
| P1  | A board-meaning concept is **silently wrong or absent** (e.g. a status that has no lane; a gate bead shown as plain work). |
| P2  | A capability is **unsurfaced** where showing it would materially improve fidelity.                            |
| P3  | Minor; narrow impact or workaround exists.                                                                    |
| P4  | Cosmetic / future-proofing only.                                                                             |

---

## Notes / open questions

Anything that needs a human decision or a cross-section dependency. Keep it
short — real follow-ups belong in the GAPS table as filed-bead candidates.
