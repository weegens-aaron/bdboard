# <Feature name>

> Copy this file to `docs/catalog/<name>.md` and replace every placeholder.
> Keep the section order below — it is the canonical shape every catalog doc
> shares so readers can scan any feature the same way. Delete this blockquote
> and the one-line prompts once you have filled in real content.

## What it shows

<!-- One line prompt: describe the user-visible thing — what the user sees on
     screen, the affordances they interact with, and what state it represents. -->

## Where the data comes from

<!-- One line prompt: trace the data path — the route(s) that serve it, the
     derive/store layer that shapes it, and the ultimate source of truth. -->

## What changes its state

<!-- One line prompt: list the actions, events, or triggers that mutate or
     refresh this feature (user actions, SSE pushes, polling, cache invalidation). -->

## Edge cases & notes

<!-- One line prompt: capture quirks, empty/zero states, error handling,
     race conditions, and any gotchas a future maintainer must know. -->

## Source files

<!-- One line prompt: list the concrete files (routes, templates, partials,
     derive modules, CSS) that implement this feature, with relative paths. -->
